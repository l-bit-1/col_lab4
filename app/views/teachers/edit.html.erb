<% content_for :title, "Editing teacher" %>

<h1 class="mb-4">Editing Teacher</h1>

<div class="card">
  <div class="card-body">
    <%= render "form", teacher: @teacher %>
  </div>
</div>

<div class="mt-3 d-flex gap-2">
  <%= link_to "Show this teacher", @teacher, class: "btn btn-outline-primary" %>
  <%= link_to "Back to teachers", teachers_path, class: "btn btn-outline-secondary" %>
</div>